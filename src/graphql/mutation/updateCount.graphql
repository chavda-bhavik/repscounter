mutation updateCount($data: CountInput!, $id: Int!) {
    updateCount(data: $data, id: $id) {
        entity {
            id
            date
            sets
            reps
            kg
            exerciseId
            exercise {
                id
                name
            }
        }
        errors {
            field
            message
        }
    }
}
