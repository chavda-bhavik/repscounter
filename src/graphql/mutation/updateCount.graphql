mutation updateCount($data: CountInput!, $id: String!) {
    updateCount(data: $data, id: $id) {
        entity {
            ...CountFields
        }
        errors {
            field
            message
        }
    }
}
