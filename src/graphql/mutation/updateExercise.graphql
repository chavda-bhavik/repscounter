mutation UpdateExercise($data: AddExerciseType!, $id: Int!) {
    updateExercise(data: $data, id: $id) {
        errors {
            field
            message
        }
        entity {
            id
            name
            calories
            target
        }
    }
}
