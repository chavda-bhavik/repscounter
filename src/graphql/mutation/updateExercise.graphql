mutation UpdateExercise($data: AddExerciseType!, $id: String!) {
    updateExercise(data: $data, id: $id) {
        errors {
            field
            message
        }
        entity {
            ...ExerciseFields
        }
    }
}
