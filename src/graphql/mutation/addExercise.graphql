mutation AddExercise($data: AddExerciseType!) {
    addExercise(data: $data) {
        entity {
            ...ExerciseFields
        }
        errors {
            field
            message
        }
    }
}
