query counts($exerciseId: Int) {
    counts(exerciseId: $exerciseId) {
        id
        date
        sets
        reps
        kg
        exerciseId
        exercise {
            name
        }
    }
}
